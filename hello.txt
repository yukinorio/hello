SELECT id, minLng, maxLng 
FROM tableName 
WHERE ((minLat <= 90 AND maxLat >= -90) OR (minLat IS NULL AND maxLat IS NULL)) 
AND ((minLng <= 180 AND maxLng >= -180) OR (minLng IS NULL AND maxLng IS NULL))
